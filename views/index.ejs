<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Pinterest Clone</title>
  <style>
    body {
      background: #fafafa;
      font-family: Arial, sans-serif;
    }
    .container {
      width: 90%;
      margin: 40px auto;
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 20px;
    }
    .card {
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.07);
      overflow: hidden;
      display: flex;
      flex-direction: column;
      min-height: 180px;
      justify-content: center;
      align-items: center;
      padding: 24px 12px;
      transition: box-shadow 0.2s;
    }
    .card:hover {
      box-shadow: 0 4px 16px rgba(0,0,0,0.15);
    }
    .card .fake-img {
      width: 100%;
      height: 120px;
      background: linear-gradient(135deg, #e0e0e0 60%, #cfcfcf 100%);
      border-radius: 12px;
      margin-bottom: 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #aaa;
      font-size: 1.2em;
      font-weight: bold;
      letter-spacing: 1px;
    }
    .card .desc {
      text-align: center;
      color: #333;
      font-size: 1em;
    }
  </style>
</head>
<body>
  <h1 style="text-align:center;">Pinterest 스타일 데모</h1>
  <div class="container">
    <% for(let i=1; i<=12; i++) { %>
      <div class="card">
        <div class="fake-img">NO IMAGE</div>
        <div class="desc">카드 <%= i %> 설명</div>
      </div>
    <% } %>
  </div>
</body>
</html>
